[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "oa_downloader"
version = "0.1.0"
description = "OA PDF downloader backend"
requires-python = ">=3.11"

[project.scripts]
oa-downloader = "oa_downloader.cli:main"
